[
    {
        "Name": "ETL Bootstrap",
        "Path": "s3://#{bucket_name}/#{etl_environment}/bootstrap/bootstrap.sh",
        "Args": [
            "#{bucket_name}",
            "#{etl_environment}"
        ]
    }
]
