[
    {
        "Name": "ETL Bootstrap",
        "Path": "s3://#{bucket_name}/#{etl_environment}/bin/bootstrap.sh",
        "Args": [
            "#{bucket_name}",
            "#{etl_environment}"
        ]
    }
]
