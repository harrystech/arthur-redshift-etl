[
    {
        "Name": "ETL Bootstrap",
        "Path": "s3://${object_store.s3.bucket_name}/${object_store.s3.prefix}/bin/bootstrap.sh",
        "Args": [
            "${object_store.s3.bucket_name}",
            "${object_store.s3.prefix}"
        ]
    }
]
